allprojects {
    apply plugin:'java'
    version = '1.0.0-SNAPSHOT'
    repositories {
        mavenCentral()
    }
}

dependencies {
    compile project(':bzh-core')
    compile project(':bzh-json')
    testCompile 'junit:junit:4.11'
}

